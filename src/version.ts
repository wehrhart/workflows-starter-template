/**
 * Version stamp shown in the app sidebar — bump the label on every release
 * so a user can confirm at a glance which build their downloaded folder is.
 * Keep the label short: a number and the headline change.
 */
export const APP_VERSION = "v19 · sturdier Distributors search";
